let name: string = "Xavi"; // Variable declared
const surname: string = "Roig"; //Constant declared

let hpPoints: number = 123;

let diferentsTypes: number | string = 4543;
diferentsTypes = 'full';

const isAlive: boolean = true;

console.log({name, surname, hpPoints, diferentsTypes, isAlive});

export{};